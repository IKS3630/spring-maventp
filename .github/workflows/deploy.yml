# Java + Configuration automatique du settings.xml
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
          server-id: nexus # Doit correspondre à l'id dans votre pom.xml
          server-username: NEXUS_USER # Nom de la variable d'env
          server-password: NEXUS_PASSWORD # Nom de la variable d'env
