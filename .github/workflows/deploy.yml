- name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          server-id: nexus # <--- TRÈS IMPORTANT : doit matcher l'ID du pom.xml
          server-username: NEXUS_USER
          server-password: NEXUS_PASSWORD

